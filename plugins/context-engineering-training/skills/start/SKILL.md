---
name: start
description: >
  Begin the Context Engineering Training. Welcomes the student, initializes workspace,
  generates the progress portal, and hands off to Session 0. Use when: the user says
  "start training", "begin training", "start course", "let's begin", or
  "start context engineering".
user-invocable: true
---

# Start Training

Welcome a new student and set up their training workspace.

## Process

### Step 1: Welcome and Orient

Introduce yourself. Keep it brief — no more than 3 paragraphs:

- This is a hands-on training program for Claude Desktop and Cowork
- You'll build a real workflow across 10 sessions — not toy exercises
- The training itself is a plugin running inside Claude, so you're already learning by doing

Then ask: **What's your name?**

🛑 **CHECKPOINT** — Wait for the student's name.

### Step 2: Environment Gate — Cowork Required

This training runs in Cowork only. In Claude Chat, the plugin's skills load but its templates and instructor agent do not — sessions will break partway through. Do not proceed in Chat.

Run both checks yourself before continuing:

1. **Bundle check** — Read `templates/STATE.md` from the plugin bundle. If you cannot reach it, you are not in an environment that serves the full plugin (this is what happens in Claude Chat).
2. **Workspace check** — Confirm a workspace folder is selected and writable. The training deploys files there; without it, nothing persists.

**If either check fails, stop.** Tell the student:

> "This training runs in Cowork, not in a regular chat. Here's how to get there: open Claude Desktop, switch to the Cowork tab, select (or create) a folder for your training, and say 'start training' again. Everything will work from there."

Do not attempt a partial start in Chat — no template deployment, no portal, no Session 0.

Also confirm: they're on a plan with Cowork access (Pro, Max, Team, or Enterprise — ask them to check if unsure).

🛑 **CHECKPOINT** — Both checks pass and plan is confirmed.

### Step 3: Initialize Workspace

Create the training workspace:

1. **Read templates/** and deploy to the student's workspace folder:
   - `STATE.md` — Set student name and start date
   - `WORKFLOW.md` — Leave blank (filled in Session 2)
   - `learning-journal.md` — Deploy empty (appended to after each session)

2. **Update STATE.md:**
   - Set Student Name
   - Set Started date (today)
   - Set Session 0 status to "In Progress"

3. **Set Position:**
   - Session: 0
   - Tier: Foundation

### Step 4: Generate Initial Portal

Invoke the `portal` skill to generate `training-portal.html` in the student's workspace. This is their progress dashboard — it updates after every session. Present it to the student as a file link so they can open it.

### Step 5: Hand Off to Session 0

Tell the student:
- Their training workspace is set up
- Session 0 is orientation and configuration — you'll see what you're building toward and get Claude dialed in
- The real hands-on learning starts in Session 1

Then invoke the `session-0` skill.

## Reads

| File | What You Need |
|------|---------------|
| `templates/STATE.md` | Template to deploy |
| `templates/WORKFLOW.md` | Template to deploy |
| `templates/learning-journal.md` | Template to deploy |

## Writes

| File | Action |
|------|--------|
| `STATE.md` | Deploy and populate with student info |
| `WORKFLOW.md` | Deploy (blank template) |
| `learning-journal.md` | Deploy (empty template) |
| `training-portal.html` | Generated by `portal` skill — student's progress dashboard |
